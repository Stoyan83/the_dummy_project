<!DOCTYPE html>
<html>
  <head>
  <%= render "shared/head" %>  
  </head>

  <body>
   <%= render 'shared/navbar' %>
   <% if current_page?(root_path) %>
   <%= render 'shared/flash' %>
   <% end %>
    <%= yield %>
   <%= turbo_frame_tag "modal" %>
  </body>
</html>
