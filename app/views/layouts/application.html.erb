<!DOCTYPE html>
<html>
  <head>
  <%= render "shared/head" %>  
  </head>

  <body>
   <%= render 'shared/navbar' %>
   <% if not devise_controller? %>
   <%= render 'shared/flash' %>
   <% end %>
    <%= yield %>
   <%= turbo_frame_tag "modal" %>
  </body>
</html>
